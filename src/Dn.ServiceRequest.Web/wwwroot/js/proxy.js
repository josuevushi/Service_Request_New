/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module app

(function(){

  // controller dn.serviceRequest.comments.comment

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.comments.comment');

    dn.serviceRequest.comments.comment.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/comment',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.comments.comment.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/comment/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.comments.comment['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/comment/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.comments.comment.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/comment/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.comments.comment.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/comment' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.familles.famille

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.familles.famille');

    dn.serviceRequest.familles.famille.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/famille',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.familles.famille.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/famille/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.familles.famille['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/famille/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.familles.famille.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/famille/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.familles.famille.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/famille' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.groupes.groupe

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.groupes.groupe');

    dn.serviceRequest.groupes.groupe.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupes.groupe.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupes.groupe['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.groupes.groupe.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.groupes.groupe.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.groupeTypes.groupeType

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.groupeTypes.groupeType');

    dn.serviceRequest.groupeTypes.groupeType.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-type',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupeTypes.groupeType.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-type/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupeTypes.groupeType['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-type/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.groupeTypes.groupeType.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-type/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.groupeTypes.groupeType.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-type' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.groupeUsers.groupeUser

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.groupeUsers.groupeUser');

    dn.serviceRequest.groupeUsers.groupeUser.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-user',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupeUsers.groupeUser.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-user/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.groupeUsers.groupeUser['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-user/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.groupeUsers.groupeUser.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-user/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.groupeUsers.groupeUser.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/groupe-user' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.pieceJointes.pieceJointe

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.pieceJointes.pieceJointe');

    dn.serviceRequest.pieceJointes.pieceJointe.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/piece-jointe',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.pieceJointes.pieceJointe.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/piece-jointe/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.pieceJointes.pieceJointe['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/piece-jointe/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.pieceJointes.pieceJointe.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/piece-jointe/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.pieceJointes.pieceJointe.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/piece-jointe' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.tickets.ticket

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.tickets.ticket');

    dn.serviceRequest.tickets.ticket.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ticket',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.tickets.ticket.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ticket/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.tickets.ticket['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ticket/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.tickets.ticket.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ticket/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.tickets.ticket.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/ticket' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dn.serviceRequest.types.type

  (function(){

    abp.utils.createNamespace(window, 'dn.serviceRequest.types.type');

    dn.serviceRequest.types.type.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/type',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.types.type.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/type/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    dn.serviceRequest.types.type['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/type/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    dn.serviceRequest.types.type.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/type/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dn.serviceRequest.types.type.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/app/type' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

})();

abp.event.trigger('abp.serviceProxyScriptInitialized');

   dn.serviceRequest.familles.famille.getList('')
        .then(function (result) {
            console.log(result);
        });